# Minimal sida med Bootstrap 4.6

En enkel demo-sida med header, en placeholder-logga (SVG) och responsiva knappar.

Filer:
- `index.html` – huvudsida
- `styles.css` – små anpassade stilar

Öppna sidan:
I PowerShell, kör:

```powershell
cd "c:\Users\noell\Downloads\ite130-info"
start .\index.html
```

Sidan använder Bootstrap 4.6 via CDN. Inga ytterligare beroenden krävs.
